Config = {}

Config = {
    Framework = 'QBCore', -- 'QBCore' or 'ESX', if using Qbox set to 'QBcore'
    UseOxLib = true,      -- Set to true to use ox_lib's lib.onCache instead of checking every five seconds
    Debug = false         -- Enable debug prints
}
